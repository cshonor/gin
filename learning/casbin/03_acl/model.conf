# 对应视频: 3.ACL访问控制
# ACL = Access Control List，主体对资源的直接权限
# 简单模型: sub 对 obj 有 act 权限

[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = r.sub == p.sub && r.obj == p.obj && r.act == p.act

